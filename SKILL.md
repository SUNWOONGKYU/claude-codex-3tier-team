---
name: claude-codex-3tier-team
description: "Claude Code와 Codex를 결합한 3계층 AI 작업팀을 편성·운용한다. 독립 작업 구역이 둘 이상이거나 병렬 처리·종합·독립 검증의 이익이 큰 임무에서 사용한다. 소대장(Opus 5 기본), Claude Code Teammate 분대장(Sonnet 5 기본), Claude Code 서브에이전트(Haiku 4.5 기본), Codex 파견 분대장(GPT-5.6 Sol·Medium 기본), Codex 서브에이전트(GPT-5.6 Terra·Medium 기본)와 작업 중 반복하는 V1·V2 이중 검증 루프를 운용한다. Fable 5는 구독제 서비스를 원칙으로 사용하고 API 사용료가 발생하는 호출은 사용자의 사전 승인을 받으며, 외부 AI 용병 7종은 웹 세션 자동화 스킬을 통해 웹 버전으로만 호출한다."
---

# Claude Code · Codex 3계층 작업팀 구성법

사람이 사용자로서 지휘를 맡고 Claude Code가 3계층 작업팀을 운영한다. Codex는 2계층 파견 분대장과 작업 미참여 V2 통합검증자로 참여한다.

이것은 Claude Code와 Codex의 공식 통합 기능이 아니라 스킬과 작업 규칙으로 두 환경을 연결한 운용 체계다. 아래 모델 구성은 현재 기본 편성이다. 모델이 바뀌어도 판단·관리·대량 실행을 업무 난도와 비용에 따라 나눠 배치한다.

이 편제는 AI가 늘어날수록 사람의 작업자 선택·개별 지시·결과 통합·검증 업무까지 늘어나는 문제를 해결한다. 사람은 목표·완료 기준·중요 예외·최종 판단을 맡고, AI 작업팀은 분해·배정·실행·검증을 맡는다.

## 1. 편제 적용 여부 판정

다음 조건 중 하나 이상을 충족하고, 편성 비용보다 품질·속도 이익이 크면 편제한다.

- 독립적으로 나눌 작업 구역이 둘 이상이다.
- 병렬 작업의 시간 이익이 크다.
- 긴 산출물이나 복수 모듈을 하나로 종합해야 한다.
- 오류가 발생했을 때 재작업 비용이 크다.
- 서로 다른 관점의 독립 검증이 필요하다.

다음 작업은 단일 세션 또는 한 분대의 서브에이전트로 처리한다.

- 파일 1~2개의 기계적 수정, 오타 교정, 단순 치환·리네임
- 앞 단계가 끝나야 다음 단계로 갈 수 있는 순차 작업
- 여러 작업자가 같은 파일에 동시에 집중해야 하는 작업
- 조회·확인만 하고 산출물이 남지 않는 작업
- 사용자와의 대화 자체

편제 적용 여부가 불명확하면 예상 작업 구역, 병렬 이익, 검증 필요성을 사용자에게 보고해 판단받는다.

## 2. 편제

| 직책 | 실체 | 기본 모델·방식 | 책임 |
|---|---|---|---|
| 사용자 | 사람 | — | 목표·완료 기준 설정, 편제 변경·중요 예외 승인, 최종 판단 |
| 소대장(1계층) | Claude Code 팀 리더 | Opus 5 기본, 업무별 변경 가능 | 업무 파악, 분해, 배정, 종합, 보고 |
| Claude Code Teammate 분대장(2계층) | Agent Teams Teammate | Sonnet 5 기본, 업무별 변경 가능 | 담당 구역 실행, 분대원 지휘, 단계별 검증 단위 제출 |
| Codex 파견 분대장(2계층) | Codex CLI | GPT-5.6 Sol·Medium 기본, 업무별 변경 가능 | 독립 임무 실행, 자체 서브에이전트 지휘, 단계별 검증 단위 제출 |
| Claude Code 서브에이전트(3계층) | Claude Code 서브에이전트 | Haiku 4.5 기본, 업무별 변경 가능 | 범위가 분명한 단일 임무 수행 후 분대장에게 보고 |
| Codex 서브에이전트(3계층) | Codex 서브에이전트 | GPT-5.6 Terra·Medium 기본, 업무별 변경 가능 | 파견 분대장의 하위 임무 수행 후 보고 |
| V1 단계검증자 | 작업 미참여 Claude Code Teammate 별도 세션 | Sonnet 5 기본, 수정 금지 역할, 업무별 변경 가능 | 작업 중 생성되는 단계별 검증 단위 즉시 검증 |
| V2 통합검증자 | 작업 미참여 Codex 별도 세션 | GPT-5.6 Sol·Medium 기본, 수정 금지 역할, 업무별 변경 가능 | 작업 중 갱신되는 누적 통합본 독립 검증 |
| 기획·전략참모 | Fable 5 | 구독제 서비스 원칙, 유료 API는 사전 승인 | 필요할 때 기획·전략 자문 |
| 외부 AI 용병 | 7종 웹 버전 | 웹 세션 자동화 스킬만 사용 | 필요할 때 외부 관점·전문 작업 제공 |

Codex 파견 분대장은 외부 AI이지만 부대원이다. 담당 구역과 완료 기준을 받고 단계별 검증 단위를 제출하며 V1 검증을 거친다. ChatGPT 웹 버전은 외부 용병이므로 Codex 파견 분대장과 구분한다.

Claude Code Teammate 분대장과 Codex 파견 분대장은 각각 한 명으로 고정하지 않는다. 독립 작업 구역과 작업량에 따라 두 종류의 분대장을 각각 복수로 편성한다. 단, 분대장 수를 늘릴 때는 병렬 처리 이익이 추가 맥락·통합 비용보다 큰지 먼저 판단한다.

## 3. 핵심 원칙

1. 업무의 성격과 난도를 파악한 뒤 작업자와 모델을 소환한다.
2. Claude Code Teammate 분대장의 모델은 소환자가 정한다. 다른 모델이 필요하면 기존 작업자를 종료하고 다시 소환한다.
3. 분대원은 자기 분대장에게만 보고한다.
4. 작업자와 검증자를 분리한다. 검증자는 결과물을 직접 수정하지 않는다.
5. 단계별 결과물이 생길 때마다 `V1 → 누적 통합 → V2`를 반복한다. 전체 작업 종료까지 검증을 미루지 않는다.
6. 외부 AI 용병은 필요한 경우에만 호출한다.
7. 비싼 모델은 판단·종합처럼 품질 차이가 중요한 자리에 집중한다.
8. 편제는 한 번 세우면 임무 완료까지 유지한다. 변경·축소·검증 생략·중도 해산에는 사용자 승인이 필요하다.

## 4. 모델 배정

- Opus 5를 소대장의 기본 모델로 사용한다. 판단, 종합, 아키텍처 결정 등 틀린 답의 비용이 큰 일에 배정한다.
- Sonnet 5를 Claude Code Teammate 분대장과 V1의 기본 모델로 사용한다. 구현, 문서 작성, 분석, 관리, 단계검증 등 주력 작업에 배정한다.
- Haiku 4.5를 Claude Code 서브에이전트의 기본 모델로 사용한다. 대량 탐색, 분류, 요약, 포맷 변환 등 범위가 분명한 물량 작업에 배정한다.
- Codex 파견 분대장: GPT-5.6 Sol·Medium을 기본값으로 사용한다. 업무의 성격과 난도에 따라 모델과 추론 강도를 변경한다.
- Codex 서브에이전트: GPT-5.6 Terra·Medium을 기본값으로 사용한다. 파견 분대장이 하위 임무의 성격과 난도에 따라 모델과 추론 강도를 변경한다. 소대장은 내부 분해 방식에 개입하지 않고 담당 구역·완료 기준·산출물만 관리한다.
- V2 통합검증자: GPT-5.6 Sol·Medium을 기본값으로 사용한다. 검증 난도에 따라 모델과 추론 강도를 변경한다.

여러 AI를 병렬로 운용하면 총토큰은 늘 수 있다. 비용 통제의 목표는 총토큰 감소가 아니라 고급 모델을 중요한 판단에 집중하고 단순 탐색·물량 작업에 쓰는 낭비를 줄이는 것이다.

## 5. 편제의 장점과 기대효과

1. 모델 사용 최적화: 판단·종합은 Opus 5, 주력 실행·관리는 Sonnet 5, 물량 작업은 Haiku 4.5에 기본 배정한다. Codex는 파견 분대장에 GPT-5.6 Sol·Medium, 분대원에 GPT-5.6 Terra·Medium을 기본 배치한다. 총사용량이 아니라 고급 모델의 단순 작업 투입을 줄이는 방식으로 비용을 통제한다.
2. 사람과 AI의 역할 분담: 사용자는 목표·완료 기준·금지 조건을 정하고 중요한 예외와 최종 결과를 판단한다. 작업 분해, 작업자 선정, 모델 배정, 소환, 병렬 실행, 검증은 AI 작업팀이 수행한다. AI가 늘어날수록 사람의 관리 업무까지 늘어나는 문제를 막는다.
3. 이중 품질 검증: 작업자와 작업 미참여 별도 세션의 검증자를 분리하고, 검증자에게 수정 금지 역할을 부여한다. V1으로 단계별 결과물을, V2로 누적 통합본을 검사해 자기검증의 사각지대와 후반 재작업을 줄인다.
4. 독립 작업 병렬화: Claude Code Teammate 분대장과 Codex 파견 분대장을 업무에 따라 각각 복수로 편성하고, 독립 구역을 각자의 컨텍스트에서 병렬 처리한다. Claude Code Teammate는 메일박스와 공유 작업목록으로 교신하고, 소대장은 결과를 누적 통합하며 충돌을 조정한다.
5. 다중 AI 활용·사용 한도 분산: Claude Code와 Codex CLI를 정규 편제로 함께 운용하고 필요한 경우 외부 AI 용병 7종을 웹서비스로 호출한다. 각 AI의 강점에 맞춰 배정하고 AI별 작업량과 사용 한도를 분산한다. 외부 용병의 웹 호출 실패 시 금지된 다른 경로로 우회하지 않는다.

## 6. 운용 절차

### 6.1 임무 접수와 편성

1. 사용자에게 목표, 완료 기준, 금지 조건, 안전 기준을 확인한다.
2. §1에 따라 편제 적용 여부를 판정한다.
3. 임무를 서로 독립적인 담당 구역으로 나눈다.
4. 각 구역의 맥락 비용, 판단 난도, 구현 성격을 보고 Claude Code Teammate 분대장과 Codex 파견 분대장 중 적합한 작업자를 고른다.
5. 독립 작업 구역과 작업량이 크고 병렬 이익이 추가 통합 비용보다 크면 두 종류의 분대장을 각각 복수로 편성한다.
6. 담당 구역, 범위, 완료 기준, 금지 조건, 산출 형식을 포함한 완결적 임무명세서를 전달한다.
7. 긴 작업은 시작 전에 검증 지점을 나눈다. 후속 작업이 의존하는 결과, 되돌리기 어려운 결정, 재작업 비용이 급증하기 전의 결과를 각각 `검증 단위`로 정하고 단계별 통과 기준을 적는다.
8. 분대장은 병렬 분해나 물량 처리의 이익이 있을 때만 분대원을 소환한다.

Codex는 코딩·구현·디버깅·테스트·배포를 우선 검토하되 자동 배정하지 않는다. 세션 맥락을 옮기는 비용이 작업 자체보다 크거나 판단 맥락이 중요한 일은 Claude Code Teammate 분대장에게 배정한다.

### 6.2 작업과 제출

분대장은 담당 임무 전체가 끝날 때까지 기다리지 않는다. 미리 정한 단계별 통과 기준을 충족한 결과물이 생기면 즉시 검증 단위로 제출한다. 요구사항안, 설계안, 장별 초안, 실행 가능한 기능 모듈, 분석 단계의 결론처럼 후속 작업의 입력이 되는 결과가 이에 해당한다.

검증 단위는 `전체 임무의 완성품`이 아니라 `현재 단계에서 다음 작업에 넘겨도 되는 완결된 결과`다. 탐색 메모, 자료 한 건, 아직 판단이나 구현이 끝나지 않은 조각은 검증 단위로 제출하지 않는다.

### 6.3 V1 단계검증

V1 대상은 작업 중 생성된 단계별 검증 단위다. 검증 단위가 제출되면 다른 분대의 작업 종료를 기다리지 않고 즉시 검사한다. `전수검증`은 모든 작업 흔적을 본다는 뜻이 아니라, 미리 정했거나 작업 중 새로 확정한 검증 단위를 빠짐없이 검사한다는 뜻이다.

V1은 다음을 검사한다.

- 담당 범위와 완료 기준 충족
- 요구 형식과 필수 구성
- 누락과 명백한 사실·구현 오류
- 금지 조건과 안전 기준 위반

통과본만 후속 작업과 누적 통합에 사용한다. 반려본은 원작업자가 수정하고 V1을 다시 거친다. 해당 결과에 의존하는 후속 작업은 V1 통과 전 시작하지 않는다.

### 6.4 소대장 누적 통합

소대장은 V1 통과본이 나올 때마다 현재 작업본에 반영해 누적 통합본을 갱신한다. 모든 분대의 작업이 끝날 때까지 기다렸다가 한꺼번에 종합하지 않는다. 결과물 간 충돌·중복·누락을 해소하고 전체 논리를 연결한다.

### 6.5 V2 통합검증

작업에 참여하지 않은 Codex가 작업 중 갱신된 누적 통합본을 검사한다. GPT-5.6 Sol·Medium을 기본값으로 하되 검증 난도에 따라 모델과 추론 강도를 변경한다. V2는 마지막 한 번만 하는 최종검증이 아니다. 누적 통합본에 실질적인 변경이 생길 때마다, 그리고 그 통합본에 의존하는 다음 단계가 시작되기 전에 수행한다.

- 전체 논리와 판단 품질
- 분대별 결과물 사이의 정합성
- 종합 과정의 누락·왜곡
- 현재 단계의 목적과 통과 기준 달성

V2가 통과시키면 다음 단계 작업을 진행하고 다시 `단계별 결과물 → V1 → 누적 통합 → V2`를 반복한다. 반려하면 소대장이 원인을 판정해 복귀 지점을 정한다.

- 누적 통합 과정의 논리·연결·누락 문제: 소대장 수정 → V2 재검증
- 검증 단위 자체의 사실·구현·통과 기준 문제: 원작업자 수정 → V1 재검증 → 소대장 재통합 → V2 재검증

독립적인 검증 단위 여러 개가 거의 동시에 나온 경우에는 한 번에 누적 통합해 같은 V2 회차에서 검사할 수 있다. 다만 후속 작업이 의존하거나 재작업 위험이 커지는 지점을 넘겨서는 안 되며, 전체 작업이 끝날 때까지 V2를 미뤄서는 안 된다.

## 7. 검증 판정과 종료

V1과 V2는 지적마다 `[심각도] [조치필요/조치불요]`를 표시하고 근거와 필수 수정사항을 적는다. 검증자는 발견·판정·보고만 한다.

- 완료 기준·금지 조건·안전 기준에 해당하면 조치필요로 판정한다.
- 기존 목록에 없는 새로운 중대한 결함을 자동으로 조치불요 처리하지 않는다. 해당 여부가 불명확하면 소대장에게 판정을 요청한다.
- 필수 수정사항이 0건이면 현재 회차의 수정·재검증만 종료하고 다음 작업 단계로 이동한다. 이후 새 검증 단위나 누적 통합 변경이 생기면 새 V1·V2 회차를 시작한다.
- 실행 시험·렌더링·실환경 확인이 완료 기준이면 재검증 횟수를 채우는 대신 별도의 완료 증거로 확인한다.
- 같은 종류의 조치필요가 두 번 이상 나오면 개별 수리를 멈추고 해당 유형 전체를 한 번에 검사·수정한 뒤 전수 판정표를 제출한다.

## 8. Fable 5 호출

- 기획·전략 판단이 필요한 경우에만 소대장이 호출한다.
- 구독제 서비스를 원칙으로 호출하고 비용은 정액 구독요금 범위에서 처리한다.
- API 사용료가 발생하는 호출은 사용자의 사전 승인을 받은 경우에만 허용한다.
- 사전 승인 없이 API로 우회하지 않는다.

## 9. 외부 AI 용병 7종

외부 AI 용병은 ChatGPT, Gemini, Grok, Perplexity, GLM, Kimi, Solar다.

1. 각 서비스의 웹 버전만 사용한다.
2. 반드시 `웹 세션 자동화` 스킬에 호출을 맡긴다.
3. 서비스명, 질문, 완료 기준을 전달하고 지시·대기·결과 회수를 한 호출 단위로 수행한다.
4. CLI, API, MCP, SDK, 직접 HTTP 요청으로 호출하지 않는다.
5. 로그인 만료, 캡차, 사용량 제한, 사이트 장애로 실패해도 다른 경로로 우회하지 않는다.
6. 실패한 서비스와 원인을 사용자에게 보고하고 다른 웹 용병을 쓸지, 해당 용병 없이 진행할지 결정받는다.
7. 기존 로그인 계정과 구독·무료 이용 범위만 사용한다. 추가 결제와 요금제 변경은 사용자 승인을 받는다.

## 10. 보고와 편제 유지

진행 보고에는 현재 편제, 담당 구역, 검증 상태를 한 줄로 적는다. 소환이 막히거나 편제를 유지할 수 없으면 직접 작업으로 조용히 대체하지 말고 사유, 대안, 손실을 사용자에게 보고해 승인받는다. 무응답은 승인으로 간주하지 않는다.

모든 검증 단위와 최신 누적 통합본이 통과한 뒤 최종 보고한다. 마지막 V2 이후 내용이 실질적으로 바뀌었다면 그 변경을 새 통합 지점으로 보고 V2를 다시 수행한다. 실질적인 변경이 없다면 마지막에 별도의 형식적 검증을 추가하지 않고, 통과 기록과 완료 증거를 확인한 뒤 사용자의 최종 판단을 받는다.

최종 보고에는 다음을 포함한다.

- 참여한 작업자와 담당 구역
- 단계별 V1·V2 판정 및 재검증 횟수
- 완료 증거
- 최종 산출물 목록

최신 누적 통합본의 V2 통과와 사용자 보고가 끝난 뒤에만 편제를 해산한다. 세션이 바뀌면 같은 임무의 편제를 다시 세워 이어간다.

## 11. Codex CLI 환경별 호출

Windows에서 Codex CLI를 호출하거나 인코딩·샌드박스·종료 마커 문제가 관련될 때만 [references/codex-windows.md](references/codex-windows.md)를 읽고 적용한다. 특정 PC 설정과 과거 사고 기록을 핵심 운용 규칙에 섞지 않는다.
