---
name: claude-codex-3tier-team
description: "Claude Code · Codex 3계층 작업팀 구성법 — 소대장(Opus 5)–분대장(Sonnet 5)–분대원(Haiku 4.5) 3계층 편제로 작업을 분해·병렬 수행하는 스킬. 모델 배정은 소환자가 결정, V①(Sonnet 팀메이트)→V②(Codex CLI) 이중 검증 게이트, Codex CLI를 파견 분대장(2계층)으로 편성해 코딩·디버깅·배포 구역 배정, Fable 5 전략참모 API 호출, 외부 AI 용병 4종(Gemini·Grok·Perplexity·GLM) CLI→웹세션→API 3순위 폴백 호출. 사용자가 '3계층 작업팀', '3계층 작업팀 구성법', 'Claude Code Codex 3계층 작업팀', '3계층 팀 구성', 'claude-codex-3tier-team', '소대장 편제로 작업해'를 요청할 때 사용."
---

# Claude Code · Codex 3계층 작업팀 구성법 (claude-codex-3tier-team)

> 표시명: **Claude Code · Codex 3계층 작업팀 구성법** (2026-08-02 사용자 지시로 정정 — 이 편제에서 실제로 도는 것은 **Claude Code(CLI)와 Codex(CLI)** 다. "ChatGPT"는 채팅 제품명이라 편제 이름에 쓰지 않는다. 2026-07-30 Codex 파견 분대장 편입 시 붙였던 "Claude·ChatGPT"에서 승계. 2026-07-11 "Claude 3계층 작업팀 구성법"에서 승계. 폴더·슬러그·에이전트 파일명도 `claude-codex-3tier-*`로 동반 개명)

소대장–분대장–분대원 3계층 편제로 임무를 분해·병렬 수행하고, 이중 검증 게이트를 통과한 산출물만 사용자에게 보고하는 작업 편제 스킬. 2계층에는 Claude 팀메이트 분대장과 함께 **외부 CLI로 뛰는 파견 분대장(Codex)** 이 선다.

## 1. 편제 (사용자 지시로만 변경 — 소대장 임의 변경 금지)

| 직책 | 실체 | 모델 | 과금 | 소환/호출 주체 |
|---|---|---|---|---|
| 사용자 | 사용자 | — | — | — |
| 소대장 (1계층) | 팀 리더, 상주 (현재 세션) | Opus 5 | 구독 | 사용자가 임무 하달 |
| 분대장 (2계층) | 팀메이트 | Sonnet 5 | 구독 | 소대장이 소환 + 모델 배정 |
| **파견 분대장 (2계층)** | **Codex CLI (외부·비상주)** | **Codex CLI 기본 모델** | **ChatGPT 구독** | **소대장이 직접 CLI 호출** |
| 분대원 (3계층) | 서브에이전트 | Haiku 4.5 기본, 필요시 Sonnet 5 | 구독 | 분대장이 소환 + 모델 배정 |
| 검증담당 V① | 팀메이트(읽기 전용) | Sonnet 5 | 구독 | 소대장 |
| **검증담당 V②** | **Codex CLI (외부·비상주, 읽기 전용)** | **Codex CLI 기본 모델** | **ChatGPT 구독** | **소대장이 직접 CLI 호출** |
| 기획·전략참모 | API 호출 (상주 없음) | Fable 5 (claude-fable-5) | API 건당 | 소대장 판단 |
| 용병 | 외부 AI 4종 | Gemini, Grok, Perplexity, GLM | 경로별 상이 | 소대장 판단 |

> **파견 분대장 = 외부 AI인데 부대원이다.** 용병(호출 대상)과 다르다. 담당 구역을 배정받고, 자기 서브에이전트를 부려 일을 분해하며, 산출물을 제출하고, Claude 분대장과 **똑같은 검증 게이트**를 통과한다 — 분대장이 하는 일을 그대로 한다. 다른 점은 실체가 Claude 팀메이트가 아니라 **외부 CLI 프로세스**라 소대장이 직접 호출한다는 것, 그리고 부리는 분대원이 Claude가 아니라 **자기 체계의 서브에이전트**라는 것뿐이다. (2026-07-30 사용자 지시로 ChatGPT를 용병에서 파견 분대장으로 전환 — 용병은 5종→4종)

**작업 흐름**: ① 임무 → ② 소대장이 분대장 소환+모델 배정 (구역 성격에 따라 파견 분대장 배정) → ③ 분대장이 필요시 분대원 소환+모델 배정 → ④ 산출물 제출 → ⑤ V①→V② 검증 통과 → 소대장 종합 → ⑥ 사용자 보고 → 팀 정리

## 2. 편제 원칙 (10개 — 위반 금지)

1. **모델 배정은 소환자가 결정한다** — 소대장→분대장, 분대장→분대원. spawn 시 `model` 매개변수로 전달한다. (파견 분대장은 예외 — Codex CLI가 자기 모델을 쓰므로 소대장이 배정하는 것은 모델이 아니라 **구역**이다.)
2. **모델은 소환 시점에 고정된다** — 사후 변경 불가. 재배정은 종료 후 재소환으로만 한다. 따라서 운용 순서는 반드시 "업무 파악 → 모델 결정 → 소환".
3. **팀메이트 중첩 소환 불가** — 분대원은 반드시 서브에이전트(Agent/Task 도구)로 편성한다. 팀메이트가 팀메이트를 만드는 구조는 금지.
4. **분대원은 컨텍스트 격리 상태로 시작** — 분대장은 소환 프롬프트에 완결적 임무명세서(대상, 범위, 기준, 보고 형식)를 담아야 한다.
5. **분대원은 자기 분대장에게만 보고하고 소멸한다.**
6. **검증자는 수정 권한이 없다** — tools를 Read, Grep, Glob으로 제한 (Edit·Write 금지). 발견·보고만 하고, 수정은 작업자(해당 분대장)가 하며, 수정본은 재검증한다 (순환 검증).
7. **검증은 2단 게이트** — V① 전수 1차(형식·기준·명백한 오류 필터) → 통과분만 V② 최종 게이트(판단 품질, 통과/반려 결정) 순차 진행.
   - **V①은 Claude 팀메이트(Sonnet 5), V②는 Codex CLI다** (2026-08-02 사용자 지시로 전환). 사유 셋: ① **최종 게이트가 가장 비싼 자리**였다 — Opus를 걷어내 구독을 분산한다 ② **Codex는 세션 맥락이 전혀 없다** — V②의 임무가 "V①이 못 본 사각을 다른 눈으로 보는 것"이므로 맥락을 모르는 쪽이 오히려 맞다. 특히 "처음 보는 사용자가 혼동하지 않는가" 류 판정에 강하다 ③ **다른 LLM 가족**이라 Claude 자기검증 편향을 벗어난다
   - 실측(2026-08-02): Codex CLI가 **PNG 화면 캡처를 판독**한다. 3단 구조·좌우 색·한글 구획명(`LLM 설정`·`도구 연결`·`지식베이스`)까지 정확히 읽었다. 31,705토큰 전량 ChatGPT 구독 소모, Claude 토큰 0.
   - **순환에는 종료 규칙이 있다** — 판정 게이트(§4.2-4~8). **조치불요는 순환을 돌리지 않는다.**
8. **용병과 참모는 부대원이 아니라 호출 대상이다** — 상주시키지 않는다. 단 **파견 분대장(Codex)은 예외** — 외부 AI지만 부대원이며, 담당 구역·산출물·검증 의무를 진다.
9. **파견 분대장에게는 강점 구역을 배정한다 (원칙)** — 코딩·구현 / 디버깅·테스트 / 사이트 배포. **단 필요한 경우 그 외 구역도 배정할 수 있다** (원칙이지 금지가 아니다). **반대로 강점 구역이어도 아래에 해당하면 Claude 분대장에게 준다.** 어느 쪽이든 **자동 배정이 아니라 소대장의 판단**이다.
   - **맥락 비용이 큰 일** — 코덱스는 세션 컨텍스트를 전혀 공유하지 않는다. 기존 코드 컨벤션·설계 결정·앞선 대화를 임무명세서에 다 옮겨야 하는데, **그 명세서가 작업 자체보다 커지면** Claude 분대장이 싸다.
   - **판단이 섞인 일** — "이 구조가 맞나"를 같이 봐야 하는 작업은 맥락을 아는 쪽이 낫다.
   - **ChatGPT 한도가 찼을 때** — Claude 한도와 별도로 걸린다(§4.5 알려진 이슈).
   - **호출 비용이 작업보다 큰 일** — 파일 준비·인코딩·마커 파싱 오버헤드가 작업 자체를 넘어서는 자잘한 건.
10. **파견 분대장은 자기 체계의 서브에이전트를 소환한다** — 분대장과 마찬가지로 소환 권한이 있다. 단 부리는 것은 Claude 분대원이 아니라 **코덱스 자기 체계 안의 서브에이전트**다. 이는 파견 분대장의 내부 운용이므로 소대장은 관여하지 않고, **구역 단위로 임무를 주고 산출물로만 받는다.** 분해 방식의 책임은 파견 분대장에게 있다.
11. 🚨 **편제는 한 번 서면 임무가 끝날 때까지 존속한다 — 깨려면 사용자 승인을 받는다.** (2026-08-02 사용자 지시 신설) 상세는 §2-1.

## 2-1. 🚨 편제 존속 규칙 (2026-08-02 사용자 지시 — 최우선)

> *"내가 조금만 방심하면 그 작업팀이 깨져버려. 한번 작업팀을 구성하라고 했으면, 그것이 깨질 때는 나한테 승인을 받도록 엄격하게 조정해야 될 것 같애."*

### 규칙 1 — 편제는 기본값이고, 유지가 기본 동작이다

전역 CLAUDE.md가 *"비중 있는 모든 작업은 이 편제로 수행한다 — 별도 지시 없어도 기본값"* 이라고 정해 두었다. **이것은 상시 유효한 사용자 지시이며, 그 자체가 「편제로 하라」는 요청이다.** 매 임무마다 새로 허락을 받아야 하는 것이 아니다.

한 번 편제가 서면 그 임무가 끝날 때까지 존속한다. 소대장이 임의로 해산하거나 직접 작업으로 내려가지 않는다.

### 규칙 2 — 「편제가 깨진다」의 정의 (넷 중 하나라도 해당하면 깨진 것)

| # | 깨짐 | 실제 모습 |
|---|---|---|
| ① | **소대장이 실작업을 한다** | 분대장 없이 소대장이 직접 파일을 고치고 코드를 쓴다 |
| ② | **구역을 소대장이 흡수한다** | "이건 작아서 내가 하지"로 담당 구역이 사라진다 |
| ③ | **검증 게이트를 건너뛴다** | V① 또는 V② 없이 산출물을 사용자에게 올린다 |
| ④ | **임무 도중 해산한다** | 완료 보고 전에 팀이 사라진다 |

①이 가장 흔하다. **소대장은 기획·지휘·검수를 하는 자리이지 실작업자가 아니다.**

### 규칙 3 — 깨야 할 사유가 생기면 멈추고 승인을 받는다

작업을 계속하면서 묻지 않는다. **멈추고** 아래 셋을 제시한 뒤 답을 기다린다.

1. 왜 깨야 하는가 (사유)
2. 깨지 않고 가는 대안이 있는가
3. 깨면 무엇을 잃는가 (검증 누락·품질 저하 지점)

### 규칙 4 — ⚠️ 무응답은 승인이 아니다 (이번 사고의 핵심)

소대장이 물었는데 **사용자가 답하지 않았다면 편제 유지가 기본값**이다. 답이 없다고 직접 작업으로 내려가지 않는다.

> **사고 기록 (2026-08-02)** — 소대장이 "분대장 2명 병렬로 갈까, 직접 갈까"를 두 번 묻고 답을 못 받자 **직접 작업을 기본값으로 삼았다.** 그대로 문구 수정 17곳·신규 모듈 작성·화면 대조를 전부 소대장이 처리했다. V①도 없었다. 사용자가 나중에 "너 지금 3계층으로 작업하고 있는 거야"라고 물어서야 드러났다.
> **교훈**: 물었는데 답이 없으면 **묻기 전 상태(편제)** 가 유지된다. 침묵을 해산 허가로 읽지 마라.

### 규칙 5 — 하네스·도구 제약으로 소환이 막히면 그것도 「깨짐」이다. 조용히 대체하지 마라

세션 설정·플러그인·훅 때문에 팀메이트 소환이 안 되는 경우가 있다. 그때 **말없이 직접 작업으로 갈아타지 않는다.** 즉시 보고한다 — 무엇이 막았는지, 어느 층에서 온 제약인지, 우회 경로가 있는지.

> **판독 주의 (2026-08-02 실사고)** — 세션 시스템 프롬프트에 `Do not call the AgentTool unless the user requested it` 류의 문장이 붙는 경우가 있다. 이걸 **전면 금지로 읽으면 안 된다.** 단서가 *"사용자가 요청하지 않으면"* 이므로, **전역 CLAUDE.md의 상시 편제 지시가 이미 그 요청에 해당한다.** 소대장이 이를 전면 금지로 오독해 편제를 세우지 못했다. 제약 문장에 조건절이 붙어 있으면 **조건이 충족되는지부터 확인**하고, 애매하면 사용자에게 문장을 그대로 보여주고 판단을 받는다.

### 규칙 6 — 편제 상태를 매 보고에 한 줄로 적는다

보고할 때마다 **지금 누가 뛰고 있는지**를 명시한다. 이 줄이 없으면 편제가 깨졌는지 사용자가 알 수 없다.

```
편제: 소대장(Opus 5) · 구역A 분대장(Sonnet 5) · 구역B 분대장(Sonnet 5) · V①(Sonnet 5) · V②(Codex)
```

소대장 단독으로 가고 있다면 그것도 그대로 적는다 — **감추지 않는다.**

```
편제: 소대장 단독 (분대장 없음 — 사유: ○○, 사용자 승인 2026-08-02)
```

### 규칙 7 — 과편성 예외는 좁게 유지한다

사소·단발·대화형 작업까지 편제를 세우라는 뜻은 아니다. 다만 **예외를 소대장이 넓게 해석하면 규칙 2-①로 미끄러진다.** 예외는 다음으로 한정한다.

- 파일 1~2개의 기계적 수정 (오타·문구 치환·단순 리네임)
- 조회·확인만 하고 산출물이 남지 않는 작업
- 사용자와의 대화 자체

**판단이 섞이거나, 산출물이 남거나, 검증이 필요하면 예외가 아니다.**

## 3. 모델 라우팅 기준표

소대장이 작업을 분해할 때 아래 기준으로 모델을 배정한다:

- **Opus 5** (`opus`): 판단·종합·아키텍처 결정. 틀린 답의 비용이 큰 자리에만. (최종 검증 게이트 V②는 2026-08-02부로 Codex로 이관 — Opus 폴백만 유지)
- **Sonnet 5** (`sonnet`): 구현·코딩·문서·리뷰 등 주력 작업 전반. 분대장 기본 모델.
- **Haiku 4.5** (`haiku`): 대량 탐색·분류·요약·포맷 변환 등 물량전. 분대원 기본 모델.
- **Fable 5 (API)** (`claude-fable-5`): 기획·전략 자문이 필요할 때만 소대장이 호출, 자문 결과만 회수.
- **Codex CLI (파견 분대장·V②)** — ★ **최고 모델 + 추론강도 `medium`** (사용자 지시 2026-08-02).
  - **모델 = 그 시점 Codex에서 설정 가능한 최고 사양.** 호출문에 **모델명을 박지 않고** `~/.codex/config.toml` 의 설정을 따른다. 모델명을 본문에 넣으면 교체될 때마다 스킬을 고쳐야 한다 — Claude는 매일 쓰니 바로 알지만 Codex까지 따라다니는 건 번거롭다
  - **추론강도(`model_reasoning_effort`) = `medium` 을 호출문에 명시한다.**
    ```
    codex exec --skip-git-repo-check -c model_reasoning_effort="medium" - ...
    ```
    - 값 집합(`low`/`medium`/`high`/`xhigh`)은 **안정적이라 박아도 노후화하지 않는다.** 모델명과 다르다
    - **`config.toml` 전역을 고치지 않는 이유** — 그 설정은 이 PC의 모든 Codex 작업에 걸린다. 스킬 안에서만 적용하려면 호출문 `-c` 가 맞다
    - 최고 모델이면 `medium` 으로도 검증·자문에는 충분하다는 판단. 더 깊이 필요한 임무는 소대장이 그때만 `high`·`xhigh` 로 올린다
  - **호출 전 모델 점검** (§4.5):
    ```powershell
    Get-Content "$env:USERPROFILE\.codex\config.toml" | Select-String "^model"
    ```
    최고 사양이 아니면 `config.toml` 의 `model` 을 올린 뒤 호출한다. **점검 결과를 보고에 남긴다.**
  - 2026-08-02 이 PC 기준: `model = "gpt-5.5"`. **이건 기록일 뿐 규격이 아니다** — 더 높은 것이 나오면 그것으로 올린다
  - Codex는 모델 목록 조회 명령이 없다(`--list-models` 없음). 최고 사양 여부는 **릴리스 노트·`codex update`** 로 확인한다
- **Codex CLI (파견 분대장) 구역**: **코딩·구현 / 디버깅·테스트 / 사이트 배포** 구역이 원칙. 그 외 구역도 필요하면 배정 가능(원칙 9). ChatGPT 구독으로 돌아가므로 Claude 토큰을 쓰지 않는다 — **구독 분산 효과**가 있다. 단 사용량 한도가 별도로 걸린다(§4.5).
- **비용 규칙**: 분대장은 자기 작업이 병렬 분해 가능하거나 대량 탐색일 때만 분대원을 소환한다. 분대원 기본은 Haiku 4.5, 판단이 섞인 작업만 Sonnet 5.

## 4. 운용 절차 (소대장 행동 규칙)

### 4.1 임무 접수 → 팀 편성
1. **업무 파악 먼저**: 임무를 읽고 작업을 담당 구역 단위로 분해한다. (모델은 소환 시 고정되므로 파악이 소환보다 먼저다.)
2. **모델 결정 + 담당자 갈래 결정**: §3 라우팅 기준표로 각 구역의 분대장 모델을 정한다 (기본 `sonnet`). 구역이 **코딩·구현 / 디버깅·테스트 / 사이트 배포**면 파견 분대장(Codex) 배정을 우선 검토한다(원칙 9). **검토이지 자동 배정이 아니다** — 넘기기 전에 이걸 자문한다: *"임무명세서에 옮겨 적을 맥락이 작업보다 크지 않은가?"* 크면 Claude 분대장에게 준다. 파견 분대장으로 갈 구역은 3·4단계 대신 **§4.5**로 간다.
3. **분대장 소환**: 팀메이트로 소환한다 — "Spawn a teammate named squad-A using the claude-codex-3tier-squad-leader agent type, model sonnet" 방식으로 역할 파일 + 이름 + 모델을 명시 배정한다 (역할 파일의 tools·model·본문이 팀메이트에 적용됨, §4.4 참조). 소환 프롬프트에 담당 구역·산출물·완료 기준을 명시. 검증담당 V①은 같은 방식으로 역할 파일을 지정해 팀메이트로 소환한다. **V②는 팀메이트가 아니라 Codex CLI 호출**이다(§4.2-2·§4.5).
4. **모델 배정 실반영 검증 (필수)**: 소환 직후 각 분대장·분대원의 실제 모델을 소환 로그/응답으로 확인한다. 과거(2026-03) `model` 매개변수가 무시되는 버그가 보고된 바 있다. 명시 배정이 /config 기본값보다 항상 우선해야 하며, 불일치 시 종료 후 재소환한다. **파견 분대장에는 이 단계가 적용되지 않는다** — 배정할 모델이 없으므로, 대신 `Get-Command codex`로 설치·로그인 상태를 확인하는 것이 같은 자리의 점검이다(§4.5).

### 4.2 검증 게이트
1. 분대장 산출물이 모이면 V①(`claude-codex-3tier-verifier-v1`, model: sonnet)을 소환해 **전수 1차 검증** (형식·기준 충족·명백한 오류). **파견 분대장(Codex) 산출물도 예외 없이 같은 게이트를 탄다** — 외부 AI라고 봐주지 않고, 그렇다고 더 얹지도 않는다. 동일 기준이다.
2. V① 통과분만 **V②(Codex CLI)** 에 넘겨 **최종 게이트** (판단 품질, 통과/반려 결정). **팀메이트로 소환하지 않는다** — §4.5 호출 규격(5종 위반 금지)으로 소대장이 직접 CLI 호출한다. 단 **읽기 전용**이므로 `--dangerously-bypass-approvals-and-sandbox` 를 붙이지 않는다(파일 읽기·이미지 판독은 기본 모드로 된다). 임무명세서에 **검증 기준·대상 경로·화면 캡처 경로**를 담고, 산출물은 넘기되 **제조 과정의 의도·내부 결정은 전달하지 않는다**(편향 차단, §7-0 2번과 동일). 보고는 한국어로 받는다.
   - V② 산출물이 없으면(호출 실패·한도 소진) **Claude 팀메이트 V②(`claude-codex-3tier-verifier-v2`, model: opus)로 폴백**한다. 역할 파일은 그 폴백용으로 유지한다.
   - ⚠️ **호출문의 요건이 낡으면 그대로 틀린 지적이 나온다.** V②는 세션 맥락이 없어 호출문에 적힌 것만 본다. 임무 중 사용자 지시로 요건이 바뀌었으면 **호출문을 반드시 갱신**한다. 어긋난 지적이 나오면 **코드가 아니라 기준을 의심하고**, 작업자에게 "이건 고치지 마라"고 명시한다. (2026-08-02 실제 발생 — 자동화 경계가 사용자 지시로 바뀌었는데 호출문이 낡아 오탐이 나왔고, 하마터면 멀쩡한 주석을 고칠 뻔했다.)
3. 반려분은 해당 분대장이 수정 → V①부터 재검증 (순환 검증). 검증자가 직접 수정하는 것은 금지. **파견 분대장 반려분만 예외적으로 소대장이 재호출한다** — 코덱스는 상주하지 않아 스스로 고칠 수 없기 때문이다(§4.5-6).
4. **판정 게이트 — 검증자는 지적마다 `[심각도] [조치필요/조치불요]`를 첫 줄에 적는다.** 심각도와 "고쳐야 하는가"는 **다른 축**이다. 사소해 보여도 고쳐야 하면 조치필요, 심각해 보여도 이번 건에서 고칠 게 없으면 조치불요다. **조치불요면 순환을 돌리지 않는다** — 후속 과제로 기록하고 통과시킨다. V①·V② 모두에 적용하며, **소환 프롬프트에 이 표기를 요구해야** 한다.
5. **조치필요의 기준은 임무마다 소대장이 정해 소환 프롬프트에 담는다.** "무엇을 어기면 반드시 고치는가"를 **구체적 목록**으로 준다. 그 목록에 없는 것은 조치불요다. **기준이 없으면 검증자가 자기 잣대로 판단해 순환이 안 끝난다.** (예 — 법원 제출 서류를 다루는 모듈: ①금지 버튼을 누르는 코드가 있다 ②산출물이 빠지거나 중복됐는데 성공으로 보고한다 ③서버 저장이 확인 안 됐는데 성공으로 보고한다 ④자격증명을 대리 입력하는 경로가 있다)
6. **중복 가드** — 이미 조치불요로 판정해 기록한 지적을 다음 회차가 다시 짚으면 **판정을 유지하고 넘어간다.** 다만 **새 근거나 새 사고 시나리오가 붙어 나오면 재판단**한다.
7. **도구 교체 신호** — **연속 2회 조치필요가 안 나오면** 정적 분석으로 더 캘 것이 없다는 뜻이다. 순환을 끝내고 **실물 시험**(실화면·실환경 반복 검증)으로 넘어간다.
8. 🚨 **동종 반복 신호 — 같은 종류의 조치필요가 2회 이상 나오면 개별 수리를 멈춘다.** 검증자가 매 회차 **같은 구조의 결함**을 다른 위치에서 찾아내고 있다면, 그것은 **개별 수리로는 수렴하지 않는다**는 뜻이다. 하나씩 고치면 회차가 무한히 늘어난다.
   - **소대장이 판정한다** — 회차마다 지적을 보고 **"이번 것이 지난 것과 같은 종류인가"** 를 본다. 검증자는 자기 회차만 보므로 이 판정을 못 한다.
   - 같은 종류로 판정되면 **작업자에게 그 종류 전체를 한 번에 훑게 한다.** 그리고 **전수 판정표**를 산출물로 요구한다 — 무엇을 봤고 무엇이 안전하다고 판단했는지. 그 표가 있어야 다음 게이트에 "전수로 봤다"를 근거로 낼 수 있고, 없으면 또 하나씩 나온다.
   - **이건 순환을 건너뛰는 것이 아니다.** 조치필요는 여전히 전부 고친다. 바뀌는 것은 **한 번에 하나씩 → 한 번에 전부** 뿐이다.
   - **실측 (2026-08-02)**: 한 문서 검증 모듈에서 "좁은 문서 하나가 넓은 필수 요건을 조용히 충족시키는" 같은 구조의 결함이 **네 회차에 걸쳐 다섯 번** 나왔다. 2~3회차에 동종임을 알 수 있었는데 규칙에 이 조항이 없어 네 회차를 개별 수리로 돌았다. 사용자가 *"규칙대로 하면 되는데 그러면 규칙이 잘못됐다는 거잖아"* 라고 지적해 신설했다.
   - ⚠️ **규칙 밖에서 때우지 마라 — 이 조항의 진짜 교훈.** 2026-08-02 실제: 소대장이 순환이 안 끝나자 "방식을 바꾸겠다"며 **규칙에 없는 판단으로** 전수 훑기를 지시했다. **결과는 옳았지만 근거가 규칙 밖에 있었다.** 사용자 지적: *"규칙대로 하면 되는데 그러면 규칙이 잘못됐다는 거잖아."* **규칙대로 했는데 안 끝나면 그건 규칙이 빠뜨린 것이다.** 규칙 밖에서 임시로 때우면 다음 사람은 그 판단을 물려받지 못하고 같은 곳에서 또 헤맨다. **규칙을 고쳐라.**
   - **같은 세션에서 소대장이 규칙을 세 번 벗어났다** — ①시간을 이유로 중간에 멈추자고 제안 ②"범위가 넓다"는 이유로 조치필요를 후속으로 미룸(V②가 뒤집음) ③규칙 밖에서 방식 변경. **규칙을 만든 사람이 가장 먼저 어긴다.** 판정 기준은 오직 **"안전 요건에 걸리는가"** 하나이고, **시간·범위·피로도는 기준이 아니다.** (§2-1의 "무응답은 승인이 아니다"와 같은 계열 — **소대장이 스스로를 통제하는 조항**이다)

> ⚠️ **이 규칙을 오해하지 마라.** "반려를 줄이는 규칙"이 **아니다.** 고쳐야 할 것은 나올 때마다 고친다. 없애는 것은 **"고칠 게 없는데 형식상 한 바퀴 더 도는 것"** 하나다. **조치필요 기준을 느슨하게 잡아 순환을 빨리 끝내려 하면 안 된다** — 기준은 임무의 성격이 정한다. 법원 제출물·의료·금융처럼 틀렸을 때 사람에게 실질적 불이익이 가는 영역은 촘촘하게 잡는다.

### 4.3 종합 · 보고 · 해산
1. V② 통과분을 소대장이 종합해 사용자에게 보고한다.
2. 보고에는 편성표(누가·어떤 모델·어떤 구역 — **파견 분대장이 뛴 구역도 명시**), 검증 결과(통과/반려/재검증 횟수), 산출물 목록을 포함한다.
3. **해산은 임무 완료 보고 이후에만 한다** (§2-1 규칙 1·2-④). 임무가 끝나지 않았는데 팀을 정리하는 것은 편제 파기이므로 사용자 승인이 필요하다.
4. 임무가 여러 세션에 걸치면 **다음 세션에서 같은 편제를 다시 세운다.** 세션이 끊겼다는 이유로 소대장 단독으로 이어가지 않는다 — 세션 교체는 해산 사유가 아니다.

### 4.4 팀메이트(Agent Teams) 공식 규격 — 소환 전 숙지 (출처: code.claude.com/docs/en/agent-teams)

**활성화·환경**
- 실험 기능: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 필요 (settings.json `env` 또는 셸 환경변수). 이 PC는 이미 활성화돼 있다 (v2.1.201 확인, 2026-07-11).
- 팀은 첫 팀메이트 소환 시 자동 생성된다 (v2.1.178+). 팀 설정: `~/.claude/teams/{팀명}/config.json`, 공유 작업목록: `~/.claude/tasks/{팀명}/`.
- 세션당 팀은 정확히 1개. 리더 교체 불가. `/resume`·`/rewind` 후 in-process 팀메이트는 복원되지 않는다 — 세션이 끊기면 재소환한다.
- Windows Terminal은 split pane 미지원 — 이 PC에서는 in-process 모드(기본)로 운용한다. 조작: ↑/↓ 팀메이트 선택, Enter 대화 열기, x 중지, Esc 복귀.

**소환·역할 파일**
- 분대장·검증담당은 서브에이전트 정의를 팀메이트에 재사용한다: "Spawn a teammate using the claude-codex-3tier-squad-leader agent type ..." 방식. 이때 역할 파일의 `tools` 제한·`model`·본문(추가 시스템 프롬프트)이 팀메이트에 적용된다. 단 `skills`·`mcpServers` 필드는 적용되지 않는다 (프로젝트/사용자 설정에서 로드됨).
- 팀메이트 소환은 리더(소대장)만 가능 — 팀메이트가 팀메이트를 소환할 수 없다 (편제 원칙 3과 일치). 분대장(팀메이트)의 분대원은 서브에이전트로 소환하되, **in-process 팀메이트는 백그라운드 서브에이전트를 못 쓴다 — 분대원은 포그라운드로 소환**시킨다.

**모델 배정 (공식 확인)**
- 팀메이트는 리더의 /model을 상속하지 않는다. 우선순위: 소환 프롬프트의 명시 배정 > /config "Default teammate model". 이 스킬은 항상 명시 배정한다.
- 팀메이트의 모델·fast mode는 소환 시점에 고정 — 사후 변경 불가 (편제 원칙 2와 일치). 팀메이트 세션의 /model·/fast는 리더에만 영향.
- 팀메이트는 리더의 effort level과 권한 설정을 소환 시점에 상속한다.

**교신·해산**
- 팀메이트 간 직접 교신: SendMessage (이름으로 지정). 공유 Task List로 자율 조정. 유휴 시 리더에 자동 알림 — 폴링 불필요.
- 해산: 리더가 종료 요청 → 팀메이트가 승인/거부 (진행 중 API 요청 완료 후 종료라 지연 가능). 팀 config는 세션 종료 시 자동 삭제, task list는 `cleanupPeriodDays`(기본 30일)로 정리.

**알려진 이슈 (운용 시 주의)**
- 팀메이트가 Task 완료 표시를 놓치면 의존 Task가 막힌다 — 소대장은 공유 작업목록 상태를 주기 점검한다.
- 팀메이트가 API 오류로 멈추면 자동 복구되지 않는다 — 소대장이 재지시하거나 재소환한다.
- 권한 승인 프롬프트는 리더 화면에서만 뜬다 — 팀메이트 간 권한 요청 릴레이 불가.
- 토큰 비용은 단일 세션보다 훨씬 높다 (팀메이트별 독립 컨텍스트) — 병렬 이득이 있을 때만 팀을 꾸린다. 순차 작업이면 서브에이전트가 낫다.

### 4.5 파견 분대장(Codex) 소환 — 소대장이 직접 CLI 호출

Claude 팀메이트가 아니므로 spawn이 아니라 **CLI 프로세스 호출**로 소환한다. 상주하지 않고 임무 단위로 뜨고 끝난다.

**소환 절차**
1. **구역 확정** — 원칙 9(강점 구역 우선, 필요시 예외)로 담당 구역을 정한다.
2. **완결적 임무명세서 작성** — 파견 분대장은 세션 컨텍스트를 **전혀 공유하지 않는다.** 원칙 4(컨텍스트 격리)가 분대원보다 더 엄격하게 적용된다. 대상 파일 경로·범위·기준·산출 형식·종료 마커를 전부 프롬프트에 담는다. 단 **"어떻게 쪼개서 하라"까지 지시하지 않는다** — 내부 분해는 파견 분대장이 자기 서브에이전트로 알아서 한다(원칙 10). 소대장은 구역과 완료 기준만 준다.
3. **호출** — 아래 규격을 지킨다. 지키지 않으면 mojibake·hang으로 실패한다 (검증된 제약, 출처: `codex-sites-배포` 스킬 실측).
4. **결과 회수 + 소대장 폴더에 저장** — 로그에서 종료 마커를 파싱해 산출물을 확보하고, **소대장이 받아서 본 작업 폴더에 그대로 저장한다.** 파견 분대장용 별도 폴더를 만들지 않는다. `C:\Temp\...`는 호출 제약(ASCII 경로) 때문에 거치는 **경유지일 뿐**이며, 임무가 끝나면 산출물은 소대장 폴더에만 남는다.
5. **검증 인계** — 회수한 산출물을 V①→V② 게이트에 올린다 (§4.2).
6. **반려 시 재호출** — 코덱스는 상주하지 않아 스스로 고치지 못한다. **소대장이 반려 사유를 임무명세서에 담아 다시 호출한다.** 수정본은 V①부터 재검증(순환 검증).

**호출 규격 (위반 금지 5종)**
- ❌ **git 저장소가 아닌 폴더에서 플래그 없이 실행** → `Not inside a trusted directory and --skip-git-repo-check was not specified.` 로 즉시 실패(exit 1). 임시 작업 폴더(`C:\Temp\...`)는 git 저장소가 아니므로 **`--skip-git-repo-check`를 반드시 붙인다.** (2026-07-30 실측)
- ❌ **한글을 stdin·인자로 직접 전달** → mojibake. **영문 프롬프트**를 쓰고, 한글 지시는 파일로 써서 경로만 참조시킨다. **단 인코딩 규격은 파일을 누가 읽느냐에 따라 정반대다 — 바로 아래 「두 경로」 참고.**
- ❌ **한글 경로에서 실행** → codex·빌드 오류. 작업물은 **ASCII 경로**(`C:\Temp\...`)로 복사해 넘긴다.
- ❌ **stdin 미종료** → `Reading additional input from stdin...` 무한 대기. 반드시 **파이프(`|`) + `-` 인자**로 EOF를 준다.
- ❌ **종료 마커 없는 임무** → 성공·실패 판별 불가. 프롬프트 말미에 마커를 강제한다.

**두 경로 — 임무명세서 인코딩이 정반대다 (2026-08-02 실사고로 확정)**

- **경로 A — stdin 파이프** (`$p | codex exec ... -`, 아래 코드 예시): 임무명세서는 **UTF-8 BOM 없이** 작성한다. (기존 지침 그대로 — 이 경로는 BOM이 있으면 오히려 문제가 된다)
- **경로 B — Codex가 파일을 직접 읽음** (호출문에 "○○.md 파일을 읽고 그 지시대로 검증/작업하라"고 지시하는 방식): 임무명세서는 **UTF-8 BOM 필수**다. Codex가 내부적으로 PowerShell(`Get-Content`)로 그 파일을 읽는데, **PowerShell 5.1은 BOM 없는 UTF-8을 시스템 ANSI 코드페이지로 오독**해 한글이 통째로 깨진다.
  ```powershell
  # BOM 있는 UTF-8로 저장 (경로 B 전용 — 경로 A와 정반대)
  [System.IO.File]::WriteAllText($path, $txt, (New-Object System.Text.UTF8Encoding $true))
  ```
  `Out-File -Encoding utf8`은 이 환경에서 BOM을 안 붙일 수 있으니 **신뢰하지 않는다.** 저장 후 **파일 첫 3바이트가 `239 187 191`인지 확인**한다.
  - **보험 한 줄** — 호출문에 *"파일을 읽을 때 한글이 깨지면 `-Encoding UTF8`을 지정해 다시 읽어라"* 를 넣는다.
  - ⚠️ **깨짐은 조용하다** — 소스 파일(코드·HTML 등)은 멀쩡히 읽히고 **지시서만** 깨질 수 있다. Codex는 깨진 지시로도 그럴듯하게 검증·작업을 진행한다. **호출 직후 출력 앞부분에서 지시서 원문이 제대로 인용되는지 눈으로 확인**한다.
  - **실사고 (2026-08-02)** — V②(Codex) 최종 게이트 중 브리핑 파일이 BOM 없이 저장돼 있었고, Codex가 PowerShell로 그 파일을 읽으면서 한글이 통째로 깨졌다(`# V??理쒖쥌 ...` 형태). 소스 파일은 정상 읽혔기 때문에 증상이 겉으로 드러나지 않았고, **판정 기준·조치필요 목록을 하나도 못 받은 채 331KB를 검증하고서야 발견해 중단했다.** 그대로 뒀으면 근거 없는 판정이 통과 도장으로 올라갈 뻔했다. 조치: 브리핑에 BOM을 박아 재기동 → 정상 인식 확인.

```powershell
# 임무명세서(한글 가능)를 UTF-8 BOM 없이 파일로
$utf8NoBom = New-Object System.Text.UTF8Encoding $false
[System.IO.File]::WriteAllText("C:\Temp\codex_task.txt", $임무명세서, $utf8NoBom)

# 호출 — 영문 프롬프트 + stdin 파이프 + '-' 인자 + 종료 마커 강제 + git 체크 우회
Set-Location "C:\Temp"          # 작업 폴더에서 실행 (codex가 여기를 workspace로 잡는다)
$p = "Read the UTF-8 text file at C:\Temp\codex_task.txt and carry out its instructions fully. Finish with a final line exactly 'TASK_DONE: <summary>' on success, or 'TASK_BLOCKED: <reason>' if blocked."
$p | codex exec --skip-git-repo-check - *> "C:\Temp\codex_log.txt"

# 결과 회수
Select-String -Path "C:\Temp\codex_log.txt" -Pattern "TASK_DONE:|TASK_BLOCKED:" | ForEach-Object { $_.Line } | Select-Object -Last 1
```

> 승인·샌드박스를 건너뛰어야 하는 임무(파일 쓰기·배포 등)에는 `--dangerously-bypass-approvals-and-sandbox`를 추가한다. **읽기·분석 임무에는 붙이지 않는다** — 필요한 만큼만 푼다. 기본 모드로도 파일 읽기와 PowerShell 조회는 된다(실측).

**실측 검증 (2026-07-30)** — 위 규격대로 디버깅 임무 1건(off-by-one 버그 탐색)을 태운 결과: 한글 임무명세서 정상 판독, 범위 준수(수정 금지 지시 지킴), 보고 형식 준수(한국어 3줄 + 수정 코드 1줄), `TASK_DONE:` 마커 정상 출력, exit 0. 토큰 34,438 소모(ChatGPT 구독). **첫 시도는 `--skip-git-repo-check` 누락으로 exit 1 — 규격 5종을 다 지켜야 돈다.**

**알려진 이슈**
- `You've hit your usage limit ... try again at HH:MM` — ChatGPT 구독의 **별도 사용량 한도**다. Claude 한도와 무관하게 따로 걸린다. 안내 시각까지 대기하거나, 해당 구역을 Claude 분대장으로 되돌린다.
- hang 발생 시 멈춘 `codex`/자식 `node` PID만 골라 `Stop-Process`.
- 호출 전 `Get-Command codex`로 설치·로그인 상태를 확인한다. 없으면 편제에서 빼고 Claude 분대장으로 대체한다.
- ★ **모델 사양 점검 (호출 전 필수)** — `-m` 으로 모델명을 박지 않는다. 대신 설정이 **그 시점 최고 사양**인지 본다:
  ```powershell
  Get-Content "$env:USERPROFILE\.codex\config.toml" | Select-String "^model"
  ```
  최고 사양이 아니면 `config.toml` 의 `model` 을 올린 뒤 호출하고, **점검 결과를 보고에 남긴다.** 모델명을 스킬에 박으면 교체될 때마다 스킬을 고쳐야 한다 — 그래서 설정으로 뺐다(§3).
  Codex는 모델 목록 조회 명령이 없다. 최고 사양 여부는 릴리스 노트·`codex update` 로 확인한다.
- ★ **Windows 샌드박스 — `elevated_windows_sandbox = false` 여야 한다** (2026-08-02 실측).
  `~/.codex/config.toml` 의 `[features] elevated_windows_sandbox` 가 **`true` 면 샌드박스 헬퍼가 아예 뜨지 않는다** — Store 앱(`OpenAI.Codex`) 쪽 승격 헬퍼를 찾는데 그 경로가 막혀 있고 payload가 Windows 명령줄 한계(32,767자)를 넘겨 `os error 206` 으로 죽는다. **읽기조차 안 된다.**
  `false` 로 내리면 **읽기 전용 샌드박스가 정상 동작**한다(이미지 판독 포함).
  ```powershell
  Get-Content "$env:USERPROFILE\.codex\config.toml" | Select-String "elevated_windows_sandbox"
  ```
  - **V②(읽기 전용)는 이 설정만으로 충분하다.** bypass 플래그 없이 샌드박스 안에서 돈다 → **원칙 6(검증자는 수정 권한 없음)이 기계적으로 보장된다**
  - **쓰기 임무는 여전히 범위 제한이 안 된다** — `-s workspace-write` + `approval_policy="never"` 를 줘도 `read-only sandbox policy rejected` 로 거부된다. Windows에서 승격 헬퍼 없이는 쓰기 권한을 못 준다. 따라서 파견 분대장의 파일 쓰기·배포 임무는 `--dangerously-bypass-approvals-and-sandbox` + **절차적 격리**(전용 cwd·명세서에 손댈 파일 명시·회수 시 폴더 밖 변경 확인)로 간다
- ★ **추론강도는 호출문에 `-c model_reasoning_effort="medium"` 로 명시한다.** 값 집합(`low`/`medium`/`high`/`xhigh`)은 안정적이라 박아도 노후화하지 않는다. `config.toml` 전역을 고치지 않는 이유는 그 설정이 이 PC의 **모든** Codex 작업에 걸리기 때문이다 — 스킬 안에서만 적용하려면 `-c` 가 맞다. 더 깊이 필요한 임무는 소대장이 그때만 `high`·`xhigh` 로 올린다.

## 5. 기획·전략참모 (Fable 5) 호출

- 팀메이트로 소환하지 않는다 (구독 세션과 API 인증 혼합 불가). 상주 없음, 건당 과금.
- **설계 전제**: 2026-07-12 이후 Fable 5는 구독 미포함(API 전용)이다. 따라서 참모는 언제나 API 호출로만 접근하며, 구독 경로 폴백은 두지 않는다.
- 소대장이 기획·전략 판단이 필요할 때만 아래 스크립트로 호출하고 응답만 회수한다:

```
python ~/.claude/skills/claude-codex-3tier-team/scripts/call-fable5.py "자문 요청 프롬프트"
# 또는 파일 입력: python .../call-fable5.py --file 질문.md
```

- API 키는 환경변수 `ANTHROPIC_API_KEY`로 관리한다. 스크립트에 하드코딩 금지.
- 키 미설정 시 스크립트가 안내 메시지를 내고 종료한다 — 이때 소대장은 사용자에게 키 설정을 요청하거나 참모 없이 진행한다.

## 6. 용병 호출 규칙 (외부 AI 4종)

> ⚠️ **Codex는 2026-07-30부로 용병에서 빠졌다.** 부대원인 **파견 분대장**으로 승격했으므로 §4.5로 간다. 용병은 Gemini·Grok·Perplexity·GLM 4종이다.

소대장 판단으로만 호출. 호출 경로 우선순위 (폴백):

**① CLI 우선 → ② 웹세션 자동화 (크롬·구독, 추가 비용 없음) → ③ API/MCP (건당 과금)**

| 용병 | ① CLI | ② 웹세션 자동화 | ③ API/MCP |
|---|---|---|---|
| Gemini | `gemini` (Gemini CLI) | gemini.google.com | Gemini API |
| Grok | Grok CLI (실시간 X 데이터 접근 가능) | grok.com | xAI API |
| Perplexity | 전용 CLI 확인 필요 — 없으면 ②로 | perplexity.ai | Perplexity API / MCP |
| GLM | 전용 CLI 없음 — 기존 코딩 CLI에 GLM Coding Plan 연동 또는 ZCode | chat.z.ai | Z.ai API (OpenAI·Anthropic 프로토콜 호환) |

폴백 로직:
1. 해당 CLI가 설치·로그인돼 있으면 CLI로 호출한다 (`Get-Command` 로 존재 확인).
2. CLI 불가 시 기존 `웹세션-자동화` 스킬(크롬, Playwright+CDP, chat 모드)을 호출해 해당 사이트에 질의한다. 신규 개발이 아니라 호출 연동이다.
3. 웹세션도 불가할 때만 API/MCP(건당 과금)로 호출하되, 과금 발생을 사용자 보고에 명시한다.

표기 규칙: 파견 분대장은 어디서나 **"Codex"** 로 쓴다. 편제에서 도는 실체가 Codex CLI이기 때문이다. **"ChatGPT"는 과금 주체를 가리킬 때만** 쓴다(예: "ChatGPT 구독으로 돌아간다"). 편제 이름·역할 설명에는 쓰지 않는다.

## 7. 부속 파일

```
📁 ~/.claude/skills/claude-codex-3tier-team/
📄 SKILL.md                      — 본 문서
📁 scripts/
📄 call-fable5.py                — Fable 5 전략참모 API 호출 (ANTHROPIC_API_KEY 환경변수)

📁 ~/.claude/agents/
📄 claude-codex-3tier-squad-leader.md  — 분대장 역할 (model: sonnet)
📄 claude-codex-3tier-verifier-v1.md   — 검증담당 V① (model: sonnet, 읽기 전용)
📄 claude-codex-3tier-verifier-v2.md   — 검증담당 V② **폴백용** (model: opus, 읽기 전용). 평시 V②는 Codex CLI(§4.2-2)
```

> 파견 분대장은 역할 파일이 없다. Claude 에이전트가 아니라 외부 CLI라서 소환 규격이 §4.5 본문에 직접 들어 있다.

## 8. 변경 이력

- **2026-08-02 (Codex 호출 인코딩 규격 보강)** — **§4.5 호출 규격에 「두 경로」 절 신설.** 기존 "임무명세서는 UTF-8 BOM 없이"는 **stdin 파이프 경로(경로 A)에서만 맞는 말**이었는데, **Codex가 파일을 직접 읽는 경로(경로 B)에서는 정반대로 BOM이 필수**임이 드러났다 — PowerShell 5.1이 BOM 없는 UTF-8을 시스템 ANSI 코드페이지로 오독하기 때문이다. BOM을 붙이는 법(`UTF8Encoding($true)`, `Out-File -Encoding utf8`은 신뢰 금지, 첫 3바이트 `239 187 191` 확인), 호출문에 넣을 보험 문구, "깨짐은 조용하다"(소스 파일은 멀쩡히 읽히고 지시서만 깨질 수 있음 — 호출 직후 지시서 원문 인용 여부를 눈으로 확인) 경고를 추가했다. **계기**: V②(Codex) 최종 게이트를 돌리는 중 브리핑 파일이 BOM 없이 저장돼 있어 Codex가 PowerShell로 읽으면서 한글이 통째로 깨졌다. 소스 파일은 정상 읽혀 증상이 드러나지 않았고, **판정 기준·조치필요 목록을 하나도 못 받은 채 331KB를 검증하고서야 발견해 중단했다** — 방치했으면 근거 없는 판정이 통과 도장으로 올라갈 뻔했다. (사용자 지시)
- **2026-08-02 (동종 반복 신호 추가)** — §4.2에 **8번 신설.** 같은 종류의 조치필요가 2회 이상 나오면 **개별 수리를 멈추고 그 종류 전체를 한 번에 훑는다** + **전수 판정표를 산출물로 요구**한다. 판정은 소대장이 한다 — 검증자는 자기 회차만 보므로 동종 여부를 못 가린다. 계기: 한 문서 검증 모듈에서 같은 구조의 결함이 **네 회차에 걸쳐 다섯 번** 나왔는데, 규칙이 매번 "조치필요니까 고쳐라"만 시켜 **개별 수리로 수렴하지 않는다는 신호를 못 읽었다.** 소대장이 임의로 "방식을 바꾸겠다"고 규칙 밖에서 때우려 했고, 사용자가 *"규칙대로 하면 되는데 그러면 규칙이 잘못됐다는 거잖아"* 라고 지적해 **규칙 자체를 고쳤다.** 교훈 — **규칙대로 했는데 안 끝나면 그건 규칙이 빠뜨린 것이다. 규칙 밖에서 때우지 말고 규칙을 고쳐라.** 같은 세션에서 소대장이 규칙을 **세 번** 벗어났다 — ①시간을 이유로 중간에 멈추자고 제안 ②"범위가 넓다"는 이유로 조치필요를 후속으로 미룸(V②가 뒤집음) ③규칙 밖에서 방식 변경. 판정 기준은 오직 "안전 요건에 걸리는가" 하나이고 시간·범위·피로도는 기준이 아니다. (사용자 지시)
- **2026-08-02 (검증 순환 종료 규칙)** — **§4.2에 판정 게이트 4~7번 신설.** 검증자가 지적마다 `[심각도] [조치필요/조치불요]`를 적고 **조치불요면 순환을 돌리지 않는다.** 조치필요 기준은 임무마다 소대장이 소환 프롬프트에 **구체적 목록**으로 담는다(기준이 없으면 검증자가 자기 잣대로 판단해 안 끝난다). 중복 가드(이미 조치불요로 판정한 지적의 재등장은 판정 유지, 새 근거가 붙으면 재판단)와 도구 교체 신호(연속 2회 조치필요 없음 → 실물 시험으로 전환)도 함께. §4.2-2에 **V② 호출문 노후화 경고** 추가 — 요건이 바뀌었는데 호출문이 낡으면 틀린 지적이 나오고, 그때는 코드가 아니라 기준을 의심해야 한다(실제 발생). 원칙 7에 종료 규칙 존재 명시. **오해 방지 문단**을 붙였다 — 반려를 줄이는 규칙이 아니고, 기준을 느슨하게 잡아 빨리 끝내려 하면 안 된다. **출처**: 같은 문제를 먼저 겪은 내부 시스템의 판정 게이트 — 거기서도 감사관이 끝없이 감사해 순환이 안 끝나는 문제를 같은 규칙으로 풀었다. **사유**: 기존 §4.2-3은 "반려분은 수정 → V①부터 재검증"만 정하고 **언제 멈추는지가 없었다.** V② 1회 호출에 10만 토큰 안팎이 든다. 계기: 법률 문서 처리 에이전트 검증에서 V②가 3회 연속 반려(3회 다 진짜였고 실제 사고와 닿아 있었다) — 순환을 돈 것은 옳았으나 **옳았음을 판단할 기준이 없었다.** (사용자 지시)
- **2026-08-02 (편제 존속 규칙 신설)** — **§2-1 신설 + 원칙 11 추가.** 한 번 선 편제는 임무 완료까지 존속하며, 깨려면 사용자 승인을 받는다. 「깨짐」을 4가지로 정의(소대장 실작업 / 구역 흡수 / 검증 게이트 생략 / 도중 해산), **무응답은 승인이 아님**을 명문화, 하네스 제약으로 소환이 막히면 조용히 직접 작업으로 대체하지 말고 즉시 보고하도록 규정, 매 보고에 **편제 상태 한 줄** 의무화, 과편성 예외를 3가지로 좁힘. §4.3에 "해산은 완료 보고 이후" + "세션 교체는 해산 사유가 아님" 추가. 계기: 소대장이 실행 방식을 두 번 묻고 답을 못 받자 직접 작업을 기본값으로 삼아 문구 수정 17곳·신규 모듈 작성을 V① 없이 단독 처리한 사고. 부수 발견 — 세션 프롬프트의 `Do not call the AgentTool unless the user requested it` 를 전면 금지로 오독했으나, 조건절이 붙은 문장이고 **전역 CLAUDE.md의 상시 편제 지시가 이미 그 요청에 해당**한다. (사용자 지시)
- **2026-08-02 (V② 이관)** — **최종 검증 게이트 V②를 Opus 팀메이트 → Codex CLI로 전환.** 편제표 V② 행 교체, 원칙 7에 사유·실측 추가, §3 라우팅에서 Opus의 "최종 검증 게이트" 제거, §4.1-3·§4.2-2 소환 절차 교체, 부속 파일에 폴백 표기. **역할 파일 `claude-codex-3tier-verifier-v2.md` 는 폴백용으로 유지**(Codex 호출 실패·한도 소진 시). 사유: ① 최종 게이트가 가장 비싼 자리였다 ② V②의 임무가 "V①이 못 본 사각을 다른 눈으로" 인데 **Codex는 세션 맥락이 없어 오히려 적합**하다(처음 보는 사용자 관점 판정에 강함) ③ 다른 LLM 가족이라 자기검증 편향 회피. 실측: Codex CLI가 PNG 캡처를 판독하고 한글 구획명까지 정확히 읽음, 31,705토큰 전량 ChatGPT 구독. (사용자 지시)
- **2026-07-30 (모델 갱신)** — Opus 5 출시 반영: 소대장·V② 모델을 **Opus 4.8 → Opus 5**로 갱신 (편제표·§3 라우팅·frontmatter·V② 역할 파일·도표).
- **2026-08-02 (배정 원칙 보완)** — 원칙 9에 **역방향 판단 기준 4개** 추가. 강점 구역(코딩·디버깅·배포)이어도 ① 맥락 비용이 큰 일 ② 판단이 섞인 일 ③ ChatGPT 한도가 찼을 때 ④ 호출 비용이 작업보다 큰 일은 Claude 분대장에게 준다. §4.1-2에도 "검토이지 자동 배정이 아니다"와 자문 문장을 명시. **강점 구역 = 자동으로 코덱스가 아니다.** (사용자 확인)
- **2026-08-02 (명칭 정정)** — **"Claude·ChatGPT" → "Claude Code · Codex"**. 이 편제에서 실제로 도는 것은 Claude Code(CLI)와 Codex(CLI)이지 채팅 제품인 ChatGPT가 아니다. 슬러그 `claude-chatgpt-3tier-team` → **`claude-codex-3tier-team`**, 에이전트 3종도 `claude-codex-3tier-*`로 동반 개명. §6 표기 규칙도 "ChatGPT는 과금 주체를 가리킬 때만"으로 정정. (사용자 지시)
- **2026-07-30 (개명)** — 표시명을 **"Claude·ChatGPT 3계층 작업팀 구성법"** 으로 변경. Codex가 편제에 들어왔으므로 이름에 반영. **폴더·슬러그·에이전트 파일명까지 전부 개명** — `claude-3tier-team` → `claude-chatgpt-3tier-team`, `claude-3tier-*` → `claude-chatgpt-3tier-*`. 전역 CLAUDE.md의 단일 출처 참조도 동반 갱신. (이 슬러그는 2026-08-02에 `claude-codex-*`로 다시 정정됨)
- **2026-07-30** — Codex CLI를 **용병 → 파견 분대장(2계층)** 으로 전환. 편제표에 행 추가, 편제 원칙 8 예외 명시 + 원칙 9·10 신설, §3 라우팅에 Codex 구역 추가, §4.5 소환 절차 신설(호출 규격 5종·실측 검증), §4.2에 반려 시 소대장 재호출 예외, §6 용병 5종→4종, V①·V② 역할 파일에 파견 분대장 검증 기준 추가. 산출물은 별도 폴더 없이 **소대장 작업 폴더**에 저장. (사용자 지시)
- **2026-07-11** — 표시명을 "Claude 3계층 작업팀 구성법"으로 개명. 폴더·슬러그는 등록 호환을 위해 유지.
